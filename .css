body {
    background: linear-gradient(90deg, #6CB200, #85C228 100%);     /* COLORE SFONDO E STILE BODY */
    font-family: Arial, Helvetica, sans-serif;
    }
    
    fieldset {
      background: #034F84; /* STILE MODULO */
      color: #fff;
      border: 3px solid grey;
     }
    
    input {    /* STILE CASELLA DI TESTO */
      font-size: 120%;
      color: #000; 
      background-color: #FFF;
      border: 3px solid grey; /* COLORE BORDI INPUT */
      border-radius: 8px;
      padding: 5px 5px 5px 30px;
      background-repeat: no-repeat;
      background-position: 8px 9px;
      display: block;
      margin-bottom: 10px;
      }

    .button {              /* STILE PULSANTE */
        cursor: pointer;
        margin: 0 0.5rem;
        font-weight: bold;
        padding: 1rem 2rem;
        border-radius: 5rem;
        text-transform: uppercase;
        transition: all 0.2s linear;
        font-family: Arial, Helvetica, sans-serif;
        border: 3px solid grey;
        color: #FFF;
        background: linear-gradient(90deg, #6CB200, #85C228 100%);
      }
    
    .button:hover {              /* STILE PULSANTE PASSAGGIO MOUSE */
      border-color: #00CCFF;
    }

    /* IMMAGINE */
    img {
      float: right;  /* LATO DESTRO */
      position: absolute;
      top: 140px;
      left: 800px;
    }
      
      
      
        
